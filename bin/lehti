#!/usr/bin/env gosh

(import (scheme base)
        (scheme process-context)
        (lehti cli))

(define (main args)
  (run args))

(main (command-line))

;; Local Variables:
;; mode: scheme
;; End:
